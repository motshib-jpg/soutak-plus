# نشر صوتك+

هذه النسخة Static HTML/CSS/JS ولا تحتاج Build.

## تشغيل محلي على Windows
اضغط `START_SOUTAK_PLUS.bat`.

## Cloudflare Pages
- Build command: فارغ
- Output directory: المجلد الذي يحتوي `index.html`
- اربط الدومين بعد نجاح الاختبار
- `_headers` و`_redirects` موجودان

## قاعدة البيانات
استخدم مشروع Supabase مستقلًا لصوتك+ ولا تستخدم قاعدة مشروع آخر.
