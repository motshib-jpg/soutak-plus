# نظام فتح المواد بإعلانات المكافأة

## القاعدة الثابتة
هذه الأعداد معتمدة داخل الكود ولا تُؤخذ من قاعدة البيانات:
- دليل البداية لصانع المحتوى: 5 إعلانات مكافأة.
- قوالب صناعة المحتوى: 5 إعلانات مكافأة.
- من الفكرة إلى أول جمهور: 10 إعلانات مكافأة.

لا توجد اشتراكات مدفوعة ولا دفع نقدي لفتح هذه المواد.

## متى يُحتسب الإعلان؟
- الإعلان العادي لا يُحتسب.
- النقر على الإعلان لا يُحتسب.
- إغلاق الإعلان قبل اكتمال المكافأة لا يُحتسب.
- تُضاف خطوة واحدة فقط عند حدث `rewardedSlotGranted`.

## Google Ad Manager
المشروع يستخدم Google Publisher Tag Rewarded Ads على الويب.
للتفعيل الحقيقي:
1. أنشئ Rewarded ad unit في Google Ad Manager.
2. ضع مسار الوحدة الحقيقي في `assets/js/config.js` داخل `ads.rewarded.adUnitPath`.
3. اجعل `ads.rewarded.enabled = true`.
4. اختبر على HTTPS وجهاز/متصفح يدعم الصيغة.

إذا لم يكن المزود مفعّلًا، يمنع الموقع تسجيل تقدم وهمي.

## تجربة المستخدم
قبل كل إعلان يظهر بوضوح:
- عدد الإعلانات المطلوب إجمالًا.
- التقدم الحالي.
- أن الإعلان سيُحتسب فقط بعد منح المكافأة.
- زر «ليس الآن» متاح دائمًا.

## التخزين
التقدم محفوظ حاليًا في LocalStorage لكل مادة. هذا مناسب للنسخة الحالية، لكنه ليس نظام تحقق خادميًا ضد مستخدم متقدم يغيّر بيانات متصفحه يدويًا. عند الحاجة لمقاومة العبث بشكل أقوى يجب إضافة تحقق خادمي مدعوم من مزود الإعلان، من دون تغيير قاعدة 5/5/10.
